DATABASE_URL="file:./dev.db"
# Optional: enables the hybrid LLM layer (free-text parsing + natural-language explanations).
# Without it, the app runs fully on the deterministic engine.
# ANTHROPIC_API_KEY="sk-ant-..."

# Production (Vercel + Turso) only:
# DATABASE_URL="libsql://your-db.turso.io"
# TURSO_AUTH_TOKEN="..."
